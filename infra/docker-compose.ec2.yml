# EC2: PostgreSQL accesible solo desde la instancia (127.0.0.1).
# Uso: docker compose -f docker-compose.yml -f docker-compose.ec2.yml up -d

services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"
